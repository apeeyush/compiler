class Program
{
    void Main(int args)
    {
        const double pi = 3.14159;
        double r;
        console.writeline(r);
        double areaCircle = pi * r * r;
        console.writeline(r,areaCircle);
    }
}
